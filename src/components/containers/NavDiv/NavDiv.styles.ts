import {style} from 'typestyle';

const styles = {
    left: style({
        width: '600px',
        gap: '10px',

        display: 'flex',
        alignItems: 'flex-start'
    }),
    right: style({
        width: '600px',
        gap: '10px',
        padding: '0px 60px 0px 0px',

        display: 'flex',
        alignItems: 'center'
    }),
}

export default styles;