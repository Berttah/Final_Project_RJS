import {style} from 'typestyle';

const styles = {
    container: style({
        width: '160px',
        height: '30px',
        padding: '0px',
        gap: '10px',

        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    }),
}

export default styles;