import {style} from 'typestyle';

const styles = {
    container: style({
        width: '100%',
        height: '830px',

        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    }),
    
}

export default styles;