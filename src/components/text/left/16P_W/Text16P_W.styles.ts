import { style } from 'typestyle';

const styles = {
  text: style({
    fontSize: '16px',
    fontFamily: '',
    fontWeight: '500',
    lineHeight: '23px',
    textAlign: 'left',
    color: '#FFFFFF',
  }),
};

export default styles;